// UserContext.js
import React from 'react';

// Initialize and export the UserContext
const UserContext = React.createContext(null); // You can set a default value, but null is fine here

export default UserContext;